# Post Editor

Admin pages at `/admin/posts`. List posts (all, not just published). Create/edit: title, slug, excerpt, body (markdown), published toggle. Delete. Uses PostRepository. Audit all create/update/delete.
