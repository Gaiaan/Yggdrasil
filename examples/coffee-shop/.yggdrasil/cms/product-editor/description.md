# Product Editor

Admin pages at `/admin/products`. List products (CRUD). Create/edit form: name, slug, description, price, imageUrl, inStock. Delete. Uses ProductRepository. Audit all create/update/delete.
